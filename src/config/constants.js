export const HEX_SIZE = 28;
export const BOARD_RADIUS = 5;

// Isometric view parameters
export const ISO_SCALE_Y = 0.5; // Vertical compression for isometric look
export const FIT_PADDING = 0.98; // Fit-to-screen padding (1.0 = no padding)

// Camera controls
export const MIN_ZOOM = 0.5;
export const MAX_ZOOM = 3;
export const ZOOM_SENSITIVITY = 0.001;
export const PINCH_SENSITIVITY = 0.01;
export const CLICK_THRESHOLD = 5; // CSS pixels to distinguish click from drag
